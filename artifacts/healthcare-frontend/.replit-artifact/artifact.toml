kind = "web"
previewPath = "/"
title = "Healthcare Launchpad"
version = "1.0.0"
id = "artifacts/healthcare-frontend"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 26071

[services.development]
run = "pnpm --filter @workspace/healthcare-frontend run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/healthcare-frontend", "run", "build" ]
publicDir = "artifacts/healthcare-frontend/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "26071"
BASE_PATH = "/"
